# Prompt

使用者需求：
1. 建立一個跟隨 CRISP-DM 流程的範例，說明如何用簡單線性回歸解題。
2. 需提供可互動的介面，讓使用者調整 `y = a * x + b + ε` 的 slope、noise、sample size 等參數。
3. 需提供 Streamlit 或 Flask 的網頁框架，以及部署建議。
4. 產出必須包含 prompt 與過程敘述，不能只有程式碼與結果。

本專案包含：`app.py` 的 Streamlit 應用、CRISP-DM 敘事於 README、此 prompt 記錄檔，以及部署步驟。
